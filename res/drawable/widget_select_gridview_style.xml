<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android" >

    <!-- 实心 -->
    <solid android:color="@color/discover_select_dialog_select_textcolor" />
    <!-- 渐变 -->
    <!--
            <gradient
                android:startColor="#ff8c00"
                android:endColor="#FFFFFF"
                android:angle="270" />
    -->
    <!-- 描边 -->
    <stroke
        android:width="1dp"
        android:color="@color/discover_select_dialog_select_textcolor" />
    <!-- 圆角 -->
    <corners
        android:bottomLeftRadius="3dp"
        android:bottomRightRadius="3dp"
        android:topLeftRadius="3dp"
        android:topRightRadius="3dp" />

    <padding
        android:bottom="1dp"
        android:left="1dp"
        android:right="0dp"
        android:top="1dp" />

</shape>