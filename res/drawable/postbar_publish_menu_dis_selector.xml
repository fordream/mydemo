<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">

    <item android:state_pressed="true"><shape>

            <!-- 实心 -->
            <solid android:color="@color/publish_topic_menu_submit_bg_dis_color" />
            <!-- 描边 -->
            <stroke android:width=".5dp" android:color="@color/publish_topic_menu_submit_bg_dis_stroke_color" />
            <!-- 圆角 -->
            <corners android:radius="3dp" />

        </shape></item>
    <item><shape>

            <!-- 实心 -->
            <solid android:color="@color/publish_topic_menu_submit_bg_dis_color" />
            <!-- 描边 -->
            <stroke android:width=".5dp" android:color="@color/publish_topic_menu_submit_bg_dis_stroke_color" />
            <!-- 圆角 -->
            <corners android:radius="3dp" />

        </shape></item>

</selector>