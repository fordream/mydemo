<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
 <item android:state_pressed="true"><shape>

            <!-- 实心 -->
            <solid android:color="@color/global_color5" />
            <!-- 描边 -->
            <stroke android:width="1dp" android:color="@color/global_color5" />
            <!-- 圆角 -->
            <corners android:radius="20dp" />
        </shape></item>
    <item android:state_checked="true"><shape>

            <!-- 实心 -->
            <solid android:color="@color/global_color5" />
            <!-- 描边 -->
            <stroke android:width="1dp" android:color="@color/global_color5" />
            <!-- 圆角 -->
            <corners android:radius="20dp" />
        </shape></item>
    <item android:state_pressed="false"><shape>
             
            <!-- 实心 -->
            <solid android:color="@color/global_color1" />
            
            <!-- 描边 -->
            <stroke android:width="1dp" android:color="@color/list_item_pre" />
            <!-- 圆角 -->
            <corners android:radius="20dp" />
        </shape></item>
    <item android:state_checked="false"><shape>

           
            <!-- 实心 -->
            <solid android:color="@color/global_color1" />
            <!-- 描边 -->
            <stroke android:width="1dp" android:color="@color/list_item_pre" />
            <!-- 圆角 -->
            <corners android:radius="20dp" />
        </shape></item>
    <item><shape>

           
            <!-- 实心 -->
            <solid android:color="@color/global_color1" />
            <!-- 描边 -->
            <stroke android:width="1dp" android:color="@color/list_item_pre" />
            <!-- 圆角 -->
            <corners android:radius="20dp" />
        </shape></item>
   

</selector>