<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">

    <item android:state_pressed="true"><shape>

            <!-- 实心 -->
            <solid android:color="@color/commit_next_btn_shap_pre" />
            <!-- 描边 -->
            <stroke android:width="1dp" android:color="@color/commit_next_btn_shap_pre" />
            <!-- 圆角 -->
            <corners android:radius="3dp" />

            <padding android:bottom="0dp" android:left="0dp" android:right="0dp" android:top="0dp" />
        </shape></item>
    <item><shape>

            <!-- 实心 -->
            <solid android:color="@color/commit_next_btn_shap_nor" />
            <!-- 渐变 -->
            <!--
            <gradient
                android:startColor="#ff8c00"
                android:endColor="#FFFFFF"
                android:angle="270" />
            -->
            <!-- 描边 -->
            <stroke android:width="1dp" android:color="@color/commit_next_btn_shap_nor" />
            <!-- 圆角 -->
            <corners android:radius="3dp" />

            <padding android:bottom="0dp" android:left="0dp" android:right="0dp" android:top="0dp" />
        </shape></item>

</selector>